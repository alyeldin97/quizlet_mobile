import 'package:flutter/material.dart';

import '../core/error_dialog.dart';

Function() handleErrorScreen(BuildContext context, Screens screen,
    {arguments}) {
  switch (screen) {
    case Screens.home:
    default:
      return () {};
  }
}
